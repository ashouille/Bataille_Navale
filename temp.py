def _ship_around()
    global horizontal, y_pos, x_pos

        # x_pos = colonne
        # y_pos = ligne

    if horizontal and x_pos + boat.lenght > self.size:
        return False

    if not horizontal and y_pos + boat.lenght > self.size:
        return False


        ship_around = ""
        for cpt in range(boat.lenght):
            if x_pos = 0:
                ship_around += self.tab[y_pos + 1][x_pos + elm]
            if x_pos + cpt < self.size:

            if y_pos + cpt <= 0:

            if y_pos + cpt < self.size:

